# --- Core Data & Math ---
pandas>=2.1.0           # Dataframes
numpy>=1.26.0           # Math operations

# --- UK Government Data Support ---
odfpy>=1.4.1            # REQUIRED for DfT .ods files
openpyxl>=3.1.2         # REQUIRED for DfT .xlsx files

# --- Machine Learning & Forecasting ---
scikit-learn>=1.3.0     # Metric & Preprocessing
xgboost>=2.0.0          # The main regression model
statsmodels>=0.14.0     # For generic time-series (ARIMA)
prophet>=1.1.5          # For seasonality/holiday handling

# --- Advanced Deep Learning Forecasting (SOTA) ---
torch>=2.1.0            # Deep Learning Backend
# neuralforecast>=1.6.4 # (Skipped: Depends on 'ray', incompatible with Py3.13)
# pytorch-lightning>=2.1.0

# --- Topological Data Analysis (TDA) ---
# giotto-tda>=0.6.0       # (Skipped: No Python 3.13 wheels available yet)
# kmapper>=2.0.1        # Alternative Mapper (Optional, giotto is more robust)

# --- Clustering & XAI ---
# hdbscan>=0.8.33       # (Using sklearn.cluster.HDBSCAN instead)
shap>=0.44.0            # Explainable AI
mapie>=0.8.0            # Conformal Prediction (Uncertainty Quantification)

# --- Data Visualization ---
matplotlib>=3.8.0       
seaborn>=0.13.0
plotly>=5.18.0          # Interactive maps for the frontend

# --- Backend API (FastAPI) ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-multipart>=0.0.6 # For handling file uploads if needed

# --- Azure Production & MLOps (Crucial) ---
gunicorn>=21.2.0        # Production server for Azure Linux
azure-storage-blob>=12.19.0 # To load model.pkl from cloud storage
azure-identity>=1.15.0      # Secure, passwordless login

# --- Frontend (Streamlit) ---
streamlit>=1.30.0

# --- Utilities ---
joblib>=1.3.2           # Model saving/loading
python-dotenv>=1.0.0    # Managing API keys/Env vars
requests>=2.31.0        # For fetching Weather API data
httpx>=0.25.0           # Async requests (for FastAPI)
